•	вызов справки — при запуске без параметров и с параметром --help;
вводим "--help" и ""
•	указание неизвестной команды вместо solve;
вводим "abc"
•	неверный набор параметров — пропущенный, лишний или неизвестный параметр;
вводим "solve -a"
•	исходные данные не являются числами;
вводим "solve -a abc -b 1 -c 1"
•	исходные данные выходят за допустимый диапазон;
вводим "solve -a 100000 -b 1 -c 1"
•	квадратное уравнение с двумя корнями;
вводим "solve -a 1 -b -3 -c 2"
•	квадратное уравнение с одним корнем;
вводим "solve -a 1 -b -2 -c 1"
•	квадратное уравнение без действительных корней;
вводим "solve -a 1 -b 0 -c 5"
•	линейное уравнение с одним корнем;
вводим "solve -a 0 -b 2 -c -4"
•	A = 0 и B = 0 — неизвестного нет, уравнения не существует при любом C; сообщение об ошибке и код возврата 1.
вводим "solve -a 0 -b 0 -c 1"
